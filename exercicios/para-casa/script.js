function descontoCalc(compra, taxaDesconto) {
    let desconto = taxaDesconto/100

    let resultado = compra- (compra * desconto)

    return resultado

}

x - (x*(15/100))


//se for somente o desconto


// function descontoCalc(compra, taxaDesconto) {

//     let desconto = taxaDesconto/100 }


